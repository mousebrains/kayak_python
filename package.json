{
  "name": "kayak-js-tests",
  "private": true,
  "description": "Playwright JS smoke tests for the kayak PHP layer. See docs/PLAN_js_smoke_tests.md.",
  "scripts": {
    "test": "playwright test",
    "test-install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@biomejs/biome": "2.5.0",
    "@playwright/test": "^1.51.0"
  }
}
