# Large generated metadata snapshots. They produce thousands of diff lines on
# every export (the geom / HUC blobs), so mark them -diff (git shows "Binary
# files differ" instead of the wall of text) and linguist-generated (GitHub
# folds them in PRs and drops them from language stats). Regenerated by
# scripts/export_metadata.py — review the DB change, not the snapshot diff.
data/db/reaches.json  -diff linguist-generated
data/db/reach.csv     -diff linguist-generated
data/db/huc_name.csv  -diff linguist-generated
