# /etc/fail2ban/filter.d/nginx-default-block.conf
# Ban any IP that repeatedly hits the default (IP-direct) server block.
# Legitimate users use the hostname; only scanners hit the bare IP.

[Definition]

failregex = ^<HOST> .*$

ignoreregex =
